#include <stdio.h>

int main() {
    int a, b;
    printf("Nhap hai so a va b: ");
    scanf("%d %d", &a, &b);
    printf("Tong hai so a va b la: %d\n", a + b);
    printf("Hieu hai so a va b la: %d\n", a - b);
    return 0;
}
